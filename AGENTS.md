# ArcBox 开发代理工作约定

适用整个仓库。产品设计规格 v1.0；开发与部署总计划 v1.1 / 2026-09-23。不要假设应用已完成或部署。

## 先读再改

依次读取 docs/00-START-HERE.md、docs/delivery/01-CODEX-PLAN.md（全阶段开发与部署总计划 v1.1）、docs/06-DECISIONS.md、对应工具规格、docs/architecture/03-CONTRACTS-AND-FUNDS.md。执行子阶段、部署检查点和 CI/CD 顺序以总计划 v1.1 为准；六工具范围与资金安全约束不因排期细化而降低。用户的新明确指令优先；发现规格冲突先记录，涉及资金安全的冲突不得自行放宽。

保留 ArcBox“六工具集合站”定位。不得重命名为 Pactlane，不添加 NodeStake 品牌，不擅自增加代币、收益、交易所、跨链桥、AI 仲裁或托管钱包。

## 分支协作（2026-09-23 用户更新）

- 长期仅保留 `dev` 和 `main`；所有后续开发、修复及文档更新直接提交到最新远端 `dev`，不再创建或恢复已删除的 `feat/*`、`docs/*` 等分支。
- 本节覆盖总计划及历史交付说明中的功能分支示例；不改变 M0—M10 范围、子阶段验收或资金安全条件。
- `dev` 通过检查后以 `dev → main` PR 提交阶段性基线。提出 PR 或询问“能否合并”不等于允许自动合并；除用户明确要求直接合并外，保留用户审查和合并步骤。不直接覆盖 `main`。
- 默认保留提交历史的 merge commit，不擅自 squash/rebase；合并后保留 `dev`。下一轮开发前读取两分支最新状态；需要同步且 `main` 是 `dev` 的后继时，按已授权范围非强制快进 `dev`；出现分叉先检查，不 force push 或重置丢失提交。
- 每个 PR 绑定明确的 head SHA、实际 CI 和未完成项。合并技术底座不等于阶段验收通过，也不等于 Cloudflare 部署或主网发布；未经明确发布授权不启用部署/公开链写工作流。

## 开发节奏

- 开始前核验最新 `dev`、`main`、未合并 PR 和 [M0 当前进度](docs/validation/M0-STATUS.md)；使用当前进度与后续确认更新历史报告中的旧状态，不篡改历史证据。
- 每轮只执行总计划中一个明确子阶段，例如 M0-B，不把整个 M0 或一个完整工具塞进一次交付。完成后给出变更文件、实际测试及对应 SHA、未通过项、PR/合并状态、部署结果和唯一下一子阶段；停止，不连续扩展到全部项目。
- 先做 M0 技术验证，再做共享骨架；不要先画完整 UI 再发现合约无法运行。
- 不在一轮中同时引入大型框架迁移、数据库迁移和资金合约升级。
- 已有文件先读后改，禁止用模板覆盖用户内容；发生远端并发更新必须重新基于最新提交合并，禁止 force push。

## 不可破坏的约束

1. 付款、退款、分账、领取，以部署白名单中的 Arc 合约状态和事件为准；D1 只是业务记录与投影。
2. USDC 业务金额使用 bigint；JSON 和数据库字段为十进制字符串。不得使用 JS 浮点乘法计算金额。
3. 原生 USDC 与 ERC-20 USDC 是同一余额，不能求和；应用调用以 ERC-20 6 位精度为基线，msg.value=0。
4. 每个支付都关联链上 orderId/规则版本。不能凭截图、URL txHash、等额转账或单个 ERC-20 Transfer 解锁商品。
5. 可退款本金与可结算收入互斥；退款/结算/领取只能终结一次。禁止管理员 sweep 用户负债。
6. 后台任务可能重复、延迟或丢失重试机会。实现 outbox、幂等、游标及补扫；不得宣称 exactly-once delivery。
7. 不存用户私钥；不把合约管理员或部署私钥放进 Workers/前端/仓库。业务签名密钥必须职责受限，不能任意转走资金。
8. 默认 pull payment；公开 withdrawFor 只能付给合约固定受益人，不能让调用者替换收款地址。
9. Demo/testnet/mainnet 的数据库、R2、域名、钱包配置完全隔离。模拟状态绝不能进入主网交易流程。
10. 不承诺零风险、免费 Gas、保证履约、自动法律合规、已有审计或稳定获得资助。

## 实现选择

TypeScript、React/Vite、Workers API、D1、R2、Queues/Cron、Solidity/Foundry 是设计建议。M0 锁定可复现版本与 lockfile；不要默认“最新版”互相兼容。不默认 Next.js SSR、VPS、Redis、Postgres、Durable Objects 或复杂工作流引擎，除非已有测量证明必要。

本次设计未生成逐页视觉母版。开始实际 UI 实现前，以 docs/03、04 确认首页、创建向导、付款页、后台四类视觉基准；不要把前面对话中的营销拼图当成像素级产品规范。

2026-09-23 M1-A 已选定任务搜索优先的首页方向，并在 `design/m1-a` 交付四类响应式原型与 [视觉验收记录](docs/validation/M1-A-STATUS.md)。M1-B 正式 UI 应以该基准及 docs/03、04 为起点；原型不是正式产品代码、云端预览或资金入口。

2026-09-24 M1-C / D0 已部署仅供演示的 `arcbox-web-demo` Worker，实际网址、部署源码/版本、浏览器检查和回退证据见 [D0 验收记录](docs/validation/M1-C-D0.md)。该环境没有资金能力或 D1/R2/Queues，不得复用为测试网/生产资金入口；当时的下一子阶段为 M2-A。

2026-09-24 Demo 预览已接入 [GitHub `dev` 自动部署](docs/validation/DEMO-AUTO-DEPLOY.md)：四项 CI 检查通过才发布同一 SHA；PR 和 `main` 不发布。仓库开关 `ARCBOX_DEMO_DEPLOY_ENABLED` 已启用，`arcbox_demo` 环境只允许 `dev`。修改 Demo Worker 前须按该自动发布边界审查；需要回退时先关闭开关，防止后续推送覆盖回退版本。

2026-09-24 M2-A 已加入独立 `workers/identity`、D1 身份/协作迁移和 `identity` 构建的 `/app` 工作区。范围、API、实际 Worker/D1/Chrome 测试和未测项见 [M2-A 验收记录](docs/validation/M2-A-STATUS.md)。普通 Demo 构建不启用身份模块；不得将全零本地 database_id 配置用于部署，不得删除既有 Demo 禁用资金的门禁。只有本地实现与测试证据，不是 Cloudflare 身份后台已上线或真实扩展已验证。当时下一子阶段为 M2-B。

2026-09-26 M2-B 已加入 `workers/orders`、追加订单迁移、严格整数金额与规则快照、幂等订单、只读交易计划、回执/事件核验、账本/outbox 和有界补扫恢复。实际测试、源码/CI 证据、API、复现与限制见 [M2-B 验收记录](docs/validation/M2-B-STATUS.md)。当前 `m2b-probe-v1` 只允许显式启用的本地模式，没有正式工具 ABI、签名报价、服务端广播、托管订单部署或新增付款页面；不能把本地探针当正式资金合约。原始事件先形成持久 inbox，再原子应用订单/账本/outbox；继续开发必须保留失败恢复、重复去重和固定资金分类。当时下一子阶段为 M2-C。

2026-09-26 M2-C 已完成受控文件不可变版本、订单文件权限、限时单次下载、outbox/Queue 任务与死信恢复、站内通知、操作日志和 `/app/operations` 的本地验收。范围、失败修复、真实运行证据与 M2-D 交接见 [M2-C 收口记录](docs/validation/M2-C-STATUS.md) 和 [操作/API 说明](docs/validation/M2-C-OPERATIONS.md)。列表完成状态必须绑定当前视图，保留延迟响应、会话刷新、日志分页及旧响应隔离测试。不得放宽受控文件、退款撤销、会话绑定和任务 generation 约束；不得将 `wrangler.platform.jsonc` 或全零数据库 ID 直接部署。浏览器使用注入测试签名器，部分业务种子明确为本地 fixture，不是真实付款或扩展钱包证据。**本阶段 PR 合并后，下一子阶段为 M2-D 云端联调与安全回归**；需要新云资源、费用、真实钱包交互或公开链写入时，按已授权范围核验并在必要处暂停，不顺带进入六工具或主网发布。

## 每个 PR 的完成定义

说明对应需求/测试 ID；列出变更与迁移；类型检查、单测、合约测试、关键 E2E 实际执行且结果可重现；无秘密/日志隐私泄露；说明权限和金额边界；代码与文档同步。无法执行的测试明确写 NOT RUN，并解释原因。未经用户明确授权，不进行真实资产转账、主网合约部署、账号付费开通或生产环境变更。
